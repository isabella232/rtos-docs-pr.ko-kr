---
title: 1장 - Azure RTOS NetX Duo SMTP 클라이언트 소개
description: SMTP(Simple Mail Transfer Protocol)는 네트워크와 인터넷 간의 메일 전송을 위한 프로토콜입니다.
author: philmea
ms.author: philmea
ms.date: 07/09/2020
ms.topic: article
ms.service: rtos
ms.openlocfilehash: f58a0c235f5c2cd108ba97afe676ffa9b66e715c
ms.sourcegitcommit: e3d42e1f2920ec9cb002634b542bc20754f9544e
ms.translationtype: HT
ms.contentlocale: ko-KR
ms.lasthandoff: 03/22/2021
ms.locfileid: "104810609"
---
# <a name="chapter-1---introduction-to-azure-rtos-netx-duo-smtp-client"></a><span data-ttu-id="e8ce1-103">1장 - Azure RTOS NetX Duo SMTP 클라이언트 소개</span><span class="sxs-lookup"><span data-stu-id="e8ce1-103">Chapter 1 - Introduction to Azure RTOS NetX Duo SMTP client</span></span>

<span data-ttu-id="e8ce1-104">SMTP(Simple Mail Transfer Protocol)는 네트워크와 인터넷 간의 메일 전송을 위한 프로토콜입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-104">The Simple Mail Transfer Protocol (SMTP) is a protocol for transferring mail across networks and the Internet.</span></span> <span data-ttu-id="e8ce1-105">신뢰할 수 있는 TCP(Transmission Control Protocol) 서비스를 사용하여 콘텐츠 전송 기능을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-105">It utilizes the reliable Transmission Control Protocol (TCP) services to perform its content transfer function.</span></span>

## <a name="netx-duo-smtp-client-requirements"></a><span data-ttu-id="e8ce1-106">NetX Duo SMTP 클라이언트 요구 사항</span><span class="sxs-lookup"><span data-stu-id="e8ce1-106">NetX Duo SMTP Client Requirements</span></span>

<span data-ttu-id="e8ce1-107">NetX Duo SMTP 클라이언트를 사용하려면 NetX Duo IP 인스턴스 및 NetX Duo 패킷 풀을 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-107">The NetX Duo SMTP Client requires creation of a NetX Duo IP instance and NetX Duo packet pool.</span></span> <span data-ttu-id="e8ce1-108">SMTP 클라이언트는 TCP 소켓을 사용하여 *잘 알려진 포트 25* 로 SMTP 서버에 연결합니다. 따라서 이전에 생성된 IP 인스턴스에서 *nx_tcp_enable* 서비스를 호출하여 먼저 TCP를 사용하도록 설정해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-108">The SMTP Client uses a TCP socket to connect to an SMTP Server on the *well-known port 25. Therefore*, TCP must first be enabled by calling the *nx_tcp_enable* service on a previously created IP instance.</span></span>

<span data-ttu-id="e8ce1-109">SMTP 클라이언트 만들기 호출(nxd_smtp_client_create)을 수행하려면 실제 메일 메시지를 전송하는 것 외에도 SMTP 명령을 서버로 전송하기 위해 이전에 만든 패킷 풀이 필요합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-109">The SMTP Client create call (nxd_smtp_client_create) requires a previously created packet pool for transmitting SMTP commands to the Server as well as for sending the actual mail message.</span></span> <span data-ttu-id="e8ce1-110">패킷 페이로드는 메일 콘텐츠의 예상 크기에 따라 달라지며 TCP, IP 헤더 및 MAC 헤더를 허용해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-110">Packet payload depends on the anticipated size of the mail contents and must allow for TCP, IP header, and MAC header.</span></span> <span data-ttu-id="e8ce1-111">(IPv6 헤더는 40바이트이고 IPv4 헤더는 20바이트입니다.)</span><span class="sxs-lookup"><span data-stu-id="e8ce1-111">(Note that the IPv6 header is 40 bytes while the IPv4 header is 20 bytes.)</span></span>

<span data-ttu-id="e8ce1-112">전체 메일 메시지가 한 패킷에 들어갈 수 없으면 SMTP 클라이언트가 메시지 나머지를 포함할 추가 패킷을 할당합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-112">If the entire mail message cannot fit in one packet, the SMTP Client allocates additional packets to contain the rest of the message.</span></span>

## <a name="netx-duo-smtp-client-constraints"></a><span data-ttu-id="e8ce1-113">NetX Duo SMTP 클라이언트 제약 조건</span><span class="sxs-lookup"><span data-stu-id="e8ce1-113">NetX Duo SMTP Client Constraints</span></span>

<span data-ttu-id="e8ce1-114">NetX Duo SMTP 프로토콜은 RFC 2821 및 2554 표준을 구현하지만 몇 가지 제약 조건이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-114">While the NetX Duo SMTP protocol implements the RFC 2821 and 2554 standards, there are some constraints:</span></span>

1. <span data-ttu-id="e8ce1-115">NetX Duo SMTP 클라이언트는 LOGIN 및 PLAIN 인증만 지원하고 CRAM-MD5 다이제스트 인증을 지원하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-115">The NetX Duo SMTP Client supports only LOGIN and PLAIN authentication, but not CRAM-MD5 digest authentication.</span></span>
2. <span data-ttu-id="e8ce1-116">NetX Duo SMTP 클라이언트 메시지는 메일 항목당 한 명의 수신자로 제한되고 SMTP 서버와의 TCP 연결당 하나의 메일 메시지로만 제한됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-116">The NetX Duo SMTP Client messages are limited to one recipient per mail item, and only one mail message per TCP connection with the SMTP server.</span></span>
3. <span data-ttu-id="e8ce1-117">VRFY, SEND, SOML, EXPN, SAML, ETRN, TURN 및 SIZE SMTP 옵션은 지원되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-117">VRFY, SEND, SOML, EXPN, SAML, ETRN, TURN and SIZE SMTP options are not supported.</span></span>
4. <span data-ttu-id="e8ce1-118">SMTP 클라이언트는 일반적으로 메일 메시지를 만들기 위해 사용되는 메일 브라우저(“메일 사용자 에이전트”)가 아닙니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-118">The SMTP Client is not mail browser (“mail user agent”) which is typically used for creating the mail message.</span></span> <span data-ttu-id="e8ce1-119">“메일 전송 에이전트”에만 해당합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-119">It is a “mail transfer agent” only.</span></span> <span data-ttu-id="e8ce1-120">RFC 2821에 지정된 대로 SMTP 전송을 위해 메일 메시지의 필수 처리를 제공합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-120">It will provide the necessary processing of the mail message body for SMTP transport as specified in RFC 2821.</span></span> <span data-ttu-id="e8ce1-121">콘텐츠에서 수신자 및 역방향 경로와 같은 올바른 구문을 확인하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-121">It does not check the contents for correct syntax e.g. the recipient and reverse pathway.</span></span> <span data-ttu-id="e8ce1-122">MIME 데이터 또는 일반 텍스트 메시지와 같이 메일 버퍼에 들어가는 내용은 제한이 없습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-122">There is no restriction what is in the mail buffer e.g. MIME data or clear text messages.</span></span> <span data-ttu-id="e8ce1-123">헤더 및 메시지 본문을 포함하기 위해 RFC 2822에 지정된 메일 메시지 형식은 SMTP 클라이언트 API의 범위를 벗어납니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-123">Mail message format, specified in RFC 2822 for including headers and message body is beyond the scope of the SMTP Client API.</span></span>

## <a name="commands-supported-by-netx-duo-smtp-client"></a><span data-ttu-id="e8ce1-124">NetX Duo SMTP 클라이언트에서 지원되는 명령</span><span class="sxs-lookup"><span data-stu-id="e8ce1-124">Commands Supported by NetX Duo SMTP Client</span></span>

<span data-ttu-id="e8ce1-125">NetX Duo SMTP 클라이언트는 SMTP 서버와의 메일 세션 중 다음 명령을 사용합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-125">The NetX Duo SMTP Client uses the following commands during a mail session with an SMTP Server.</span></span>

- <span data-ttu-id="e8ce1-126">**EHLO** 클라이언트가 SMTP 서버에서 제공되는 일부 또는 모든 확장 프로토콜 SMTP 서비스를 포함하는 세션을 시작하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-126">**EHLO** The Client would like to initiate a session that includes some or all extension protocol SMTP services available from the SMTP Server.</span></span> <span data-ttu-id="e8ce1-127">이것이 기본값입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-127">This is the default.</span></span>
- <span data-ttu-id="e8ce1-128">**HELO** 클라이언트가 기본 SMTP 서비스로 제한되는 세션을 시작하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-128">**HELO** The Client would like to initiate a session limited to basic SMTP services.</span></span>
- <span data-ttu-id="e8ce1-129">**MAIL** 클라이언트는 서버가 클라이언트 메일을 수신하도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-129">**MAIL** The Client would like the Server to receive Client mail.</span></span>
- <span data-ttu-id="e8ce1-130">**AUTH** 클라이언트가 서버의 인증을 시작하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-130">**AUTH** The Client would like to initiate authentication by the Server.</span></span>
- <span data-ttu-id="e8ce1-131">**RCPT** 클라이언트가 메일을 전달하려는 다른 호스트의 사서함을 제출하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-131">**RCPT** The Client would like to submit a mailbox of another host it would like the mail to be delivered to.</span></span>
- <span data-ttu-id="e8ce1-132">**DATA** 클라이언트가 서버에 메일 메시지 데이터 전송을 시작하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-132">**DATA** The Client would like to initiate sending mail message data to the Server.</span></span>
- <span data-ttu-id="e8ce1-133">**QUIT** 클라이언트가 세션을 종료하려고 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-133">**QUIT** The Client would like to terminate the session.</span></span>

## <a name="getting-started"></a><span data-ttu-id="e8ce1-134">시작하기</span><span class="sxs-lookup"><span data-stu-id="e8ce1-134">Getting Started</span></span>

<span data-ttu-id="e8ce1-135">SMTP 클라이언트 애플리케이션은 IP 인스턴스를 만들고 이 IP 인스턴스에서 TCP를 사용하도록 설정합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-135">The SMTP Client application creates an IP instance and an enables TCP on that IP instance.</span></span> <span data-ttu-id="e8ce1-136">그런 다음, 다음 서비스를 사용하여 SMTP 클라이언트를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-136">It then creates the SMTP Client using the following service:</span></span>

```C
UINT nxd_smtp_client_create(NX_SMTP_CLIENT *client_ptr,
    NX_IP *ip_ptr, NX_PACKET_POOL
    *client_packet_pool_ptr,
    CHAR *username, CHAR *password,
    CHAR *from_address,
    CHAR *client_domain,
    UINT authentication_type,
    NXD_ADDRESS *server_address, UINT port);
```

<span data-ttu-id="e8ce1-137">*client_packet_pool_ptr* 은 SMTP 클라이언트가 SMTP 서버에 메시지를 보내기 위해 사용하는 이전에 생성된 패킷 풀에 대한 포인터입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-137">The *client_packet_pool_ptr* is a pointer to a previously created packet pool the SMTP Client will use to send messages to the SMTP Server.</span></span>

<span data-ttu-id="e8ce1-138">애플리케이션은 로컬 디바이스에 대한 *from_address* 와 서버 IP 주소를 제공해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-138">Note that an application must provide a *from_address* for the local device and a server IP address.</span></span> <span data-ttu-id="e8ce1-139">모든 주소는 정규화된 도메인 이름이어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-139">All addresses must be fully qualified domain names.</span></span> <span data-ttu-id="e8ce1-140">정규화된 도메인 이름에는 ‘@’ 문자로 구분된 로컬 부분과 도메인 이름이 포함됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-140">A fully qualified domain name contains a local-part and a domain name, separated by an ‘@’ character.</span></span> <span data-ttu-id="e8ce1-141">SMTP 클라이언트는 아래 nx_smtp_mail_send 서비스에서 *from_address* 또는 *recipient_address* 의 구문을 확인하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-141">Note that the SMTP Client does not check the syntax of the *from_address* or the *recipient_address* in the nx_smtp_mail_send service below.</span></span>

<span data-ttu-id="e8ce1-142">SMTP 클라이언트가 생성된 후 SMTP 클라이언트 애플리케이션은 올바른 형식의 SMTP 메일 메시지를 사용하여 메일 항목을 만들고, 다음 API를 사용하여 SMTP 클라이언트에 대해 메일 항목 보내기 요청을 수행합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-142">After the SMTP Client is created, the SMTP Client application creates a mail item with a properly formatted SMTP mail message, and makes the mail item send request to the SMTP Client using the following API:</span></span>

```C
UINT nx_smtp_mail_send(NX_SMTP_CLIENT *client_ptr,
    CHAR *recipient_address, UINT priority,
    CHAR *subject, CHAR *mail_body,
    UINT mail_body_length);
```

<span data-ttu-id="e8ce1-143">기본적으로 사용자 관점에서 볼 때는 IPv4 또는 IPv6으로 SMTP 클라이언트를 실행할 때 차이가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-143">There is essentially no difference in running SMTP Client over IPv4 or IPv6 from user perspective.</span></span> <span data-ttu-id="e8ce1-144">두 IP 프로토콜 간의 차이는 기본 NetX Duo 레이어에서 처리됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-144">Differences between the two IP protocols are handled in the underlying NetX Duo layer.</span></span>

<span data-ttu-id="e8ce1-145">메일을 보내려는 애플리케이션은 *nx_smtp_client_mail* 호출에 수신자 주소를 제공해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-145">Note that an application wishing to send mail must provide a recipient address in the *nx_smtp_client_mail* call.</span></span>

<span data-ttu-id="e8ce1-146">인증을 위해서는 사용자 이름이 정규화된 도메인 이름이거나 사용자 이름을 표시할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-146">For authentication, usernames can either be fully qualified domain names, or display user names.</span></span> <span data-ttu-id="e8ce1-147">이것은 서버의 인증 수행 방법에 따라 달라집니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-147">This depends on how the Server performs authentication.</span></span>

<span data-ttu-id="e8ce1-148">이 사용자 가이드의 뒷부분에 나오는 작은 예제 섹션의 데모에서는 메시지의 형식 지정 방법을 보여줍니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-148">The demo in the Small Example section later in this User Guide shows how the message should be formatted.</span></span> <span data-ttu-id="e8ce1-149">메일 항목 보내기에 성공한 경우 상태는 NX_SUCCESS입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-149">The status if the mail item was successfully sent will be NX_SUCCESS.</span></span> <span data-ttu-id="e8ce1-150">내부 오류, 중단된 TCP 연결, 서버 회신 오류 코드 수신과 같은 오류가 발생하면 *nx_smtp_mail_send* 가 0이 아닌 오류 상태를 반환합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-150">If an error occurs, whether it is an internal error, a broken TCP connection or receiving a Server reply error code, *nx_smtp_mail_send* will return a non-zero error status.</span></span>

<span data-ttu-id="e8ce1-151">메일 항목을 전송할 때 NetX Duo SMTP 클라이언트는 SMTP 서버와 새로운 TCP 연결을 만들고 SMTP 세션을 시작합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-151">When sending a mail item, NetX Duo SMTP Client creates a new TCP connection with the SMTP server and begins an SMTP session.</span></span> <span data-ttu-id="e8ce1-152">이 세션에서 클라이언트는 SMTP 프로토콜의 일부로 SMTP 서버에 일련의 명령을 보내고, 실제 메일 메시지 전송을 완료합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-152">In this session, the Client sends a series of commands to the SMTP Server as part of the SMTP protocol, culminating in sending out the actual mail message.</span></span> <span data-ttu-id="e8ce1-153">그런 다음, SMTP 세션의 결과에 관계없이 TCP 연결이 종료됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-153">The TCP connection is then terminated, regardless of the outcome of the SMTP session.</span></span>

<span data-ttu-id="e8ce1-154">성공 또는 실패에 관계없이 메일 전송 후 SMTP 클라이언트가 ‘초기’ 상태로 돌아가고, 다른 메일 전송 세션에 사용될 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-154">After mail transmission, regardless of success or failure, the SMTP Client is returned to the ‘initial’ state, and can be used for another mail transfer session.</span></span>

## <a name="netx-duo-smtp-authentication"></a><span data-ttu-id="e8ce1-155">NetX Duo SMTP 인증</span><span class="sxs-lookup"><span data-stu-id="e8ce1-155">NetX Duo SMTP Authentication</span></span>

<span data-ttu-id="e8ce1-156">인증은 SMTP 클라이언트가 SMTP 서버에 대해 자신의 ID를 입증하고 신뢰할 수 있는 사용자로서 자신의 메일이 전달될 수 있게 해주는 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-156">Authentication is a way for SMTP Clients to prove their identity to the SMTP Server and have their mail delivered as trusted users.</span></span> <span data-ttu-id="e8ce1-157">대부분의 상용 SMTP 서버에서는 클라이언트가 인증을 받아야 합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-157">Most commercial SMTP Servers require that Clients be authenticated.</span></span>

<span data-ttu-id="e8ce1-158">일반적으로 인증 데이터는 보낸 사람의 사용자 이름과 암호로 구성됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-158">Typically, authentication data consists of the sender's username and password.</span></span> <span data-ttu-id="e8ce1-159">인증 질문 중에 서버가 이 정보를 요청하고 클라이언트는 인코딩된 형식으로 요청된 데이터를 전송하여 응답합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-159">During an authentication challenge, the Server prompts for this information and the Client responds by sending the requested data in encoded format.</span></span> <span data-ttu-id="e8ce1-160">서버는 데이터를 디코딩하고 사용자 데이터베이스에서 일치하는 항목을 찾습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-160">The Server decodes the data and attempts to find a match in its user database.</span></span> <span data-ttu-id="e8ce1-161">항목이 있으면 서버에서 인증이 성공했음을 나타냅니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-161">If found, the Server indicates the authentication is successful.</span></span> <span data-ttu-id="e8ce1-162">SMTP 인증은 [RFC 2554](http://www.ietf.org/rfc/rfc2554.txt)에 정의되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-162">SMTP authentication is defined in [RFC 2554](http://www.ietf.org/rfc/rfc2554.txt).</span></span>

<span data-ttu-id="e8ce1-163">인증에는 *기본* 및 *다이제스트* 라는 두 가지 종류가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-163">There are two flavors of authentication, namely *basic* and *digest*.</span></span> <span data-ttu-id="e8ce1-164">다이제스트는 현재 NetX Duo SMTP 클라이언트에서 지원되지 않으며 여기에서 다루지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-164">Digest is not supported in the current NetX Duo SMTP Client, and will not be discussed here.</span></span> <span data-ttu-id="e8ce1-165">기본 인증은 위에서 설명한 *이름* 및 *암호* 인증과 동일합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-165">Basic authentication is equivalent to the *name* and *password* authentication described above.</span></span> <span data-ttu-id="e8ce1-166">SMTP 기본 인증에서 이름과 암호는 base64로 인코딩됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-166">In SMTP basic authentication, the name and passwords are base64 encoded.</span></span> <span data-ttu-id="e8ce1-167">기본 인증의 장점은 구현이 쉽고 널리 사용된다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-167">The advantage of basic authentication is its ease of implementation and widespread use.</span></span> <span data-ttu-id="e8ce1-168">기본 인증의 주요 단점은 이름과 암호가 요청에서 공개적으로 전송된다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-168">The main disadvantage of basic authentication is name and password data is transmitted openly in the request.</span></span>

### <a name="plain-authentication"></a><span data-ttu-id="e8ce1-169">일반 인증</span><span class="sxs-lookup"><span data-stu-id="e8ce1-169">Plain Authentication</span></span>

<span data-ttu-id="e8ce1-170">NetX Duo SMTP 클라이언트는 PLAIN 매개 변수로 AUTH 명령을 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-170">The NetX Duo SMTP Client sends an AUTH command with the PLAIN parameter.</span></span> <span data-ttu-id="e8ce1-171">NetX Duo SMTP 서버가 이 유형의 인증을 지원할 경우 334 회신 코드로 회신합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-171">If the NetX Duo SMTP Server supports this type of authentication, it will reply with a 334 reply code.</span></span> <span data-ttu-id="e8ce1-172">클라이언트는 단일 base64로 인코딩된 사용자 이름 및 암호 메시지로 서버에 회신합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-172">The Client replies with a single base64 encoded username and password message to the Server.</span></span> <span data-ttu-id="e8ce1-173">서버에서 클라이언트 인증이 성공한 것으로 확인되면 235 성공 코드로 응답합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-173">If the Server determines the Client authentication is successful, it responds with the 235 success code.</span></span>

### <a name="login-authentication"></a><span data-ttu-id="e8ce1-174">로그인 인증</span><span class="sxs-lookup"><span data-stu-id="e8ce1-174">Login Authentication</span></span>

<span data-ttu-id="e8ce1-175">NetX Duo SMTP 클라이언트는 LOGIN 매개 변수로 AUTH 명령을 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-175">The NetX Duo SMTP Client sends an AUTH command with the LOGIN parameter.</span></span> <span data-ttu-id="e8ce1-176">NetX Duo SMTP 서버가 이 유형의 인증을 지원할 경우 인증 ‘질문’이 시작될 때 334 회신 코드로 회신합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-176">If the NetX Duo SMTP Server supports this type of authentication, it will reply with a 334 reply code as the start of the authentication ‘challenge’.</span></span> <span data-ttu-id="e8ce1-177">일반적으로 “사용자 이름”인 base64로 인코딩된 프롬프트를 클라이언트로 다시 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-177">It sends a base64 encoded prompt back to the Client which is typically “Username”.</span></span> <span data-ttu-id="e8ce1-178">클라이언트가 프롬프트를 디코딩하고 base64로 인코딩된 사용자 이름으로 회신합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-178">The Client decodes the prompt, and replies with a base64 encoded username.</span></span> <span data-ttu-id="e8ce1-179">서버가 클라이언트 사용자 이름을 수락하면 클라이언트 암호에 대해 base64로 인코딩된 프롬프트를 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-179">If the Server accepts the Client username, it sends out a base64 encoded prompt for the Client password.</span></span> <span data-ttu-id="e8ce1-180">클라이언트가 base64로 인코딩된 암호로 응답합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-180">The Client responds with a base64 encoded password.</span></span> <span data-ttu-id="e8ce1-181">서버에서 클라이언트 인증이 성공한 것으로 확인되면 235 성공 코드로 응답합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-181">If the Server determines the Client authentication is successful, it responds with the 235 success code.</span></span>

### <a name="no-authentication"></a><span data-ttu-id="e8ce1-182">인증 없음</span><span class="sxs-lookup"><span data-stu-id="e8ce1-182">No Authentication</span></span>

<span data-ttu-id="e8ce1-183">일부 SMTP 서버는 인증 없이 구성됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-183">Some SMTP Servers are configured without authentication.</span></span> <span data-ttu-id="e8ce1-184">그럴 경우 클라이언트 EHLO 메시지에 대한 250 응답에는 인증 유형이 나열되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-184">If so, their 250 response to the Client EHLO message will not list any authentication types.</span></span> <span data-ttu-id="e8ce1-185">하지만 나열된 인증 유형이 없다고 해서 서버에 반드시 인증이 필요하지 않거나 인증이 지원되지 않는 것은 아닙니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-185">However, no authentication types listed does not necessarily mean the Server does not require or support authentication.</span></span> <span data-ttu-id="e8ce1-186">이러한 상황에서 클라이언트가 PLAIN 또는 LOGIN 인증으로 구성된 경우 NetX Duo 클라이언트 스레드 작업은 기본적으로 PLAIN으로 설정됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-186">If the Client is configured for PLAIN or LOGIN authentication in this situation, the NetX Duo Client thread task will default to PLAIN.</span></span> <span data-ttu-id="e8ce1-187">클라이언트가 NONE으로 구성된 경우 인증 단계를 건너뛰고 SMTP 상태가 MAIL 상태로 이동합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-187">If the Client is configured for NONE, the authentication step is skipped and the SMTP state advances to the MAIL state.</span></span>

<span data-ttu-id="e8ce1-188">클라이언트가 인증 없음으로 구성되었고 SMTP 서버가 인증을 지원하지 않을 경우 클라이언트 인증 유형이 PLAIN으로 전환됩니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-188">Note that if the Client is configured for no authentication and the SMTP Server does support authentication, the Client authentication type is switched to PLAIN.</span></span>

## <a name="rfcs-supported-by-netx-duo-smtp-client"></a><span data-ttu-id="e8ce1-189">NetX Duo SMTP 클라이언트에서 지원되는 RFC</span><span class="sxs-lookup"><span data-stu-id="e8ce1-189">RFCs Supported by NetX Duo SMTP Client</span></span>

<span data-ttu-id="e8ce1-190">NetX Duo SMTP 클라이언트 API는 RFC2821 “Simple Mail Transfer Protocol” 및 RFC 2554 “인증에 대한 SMTP 서비스 확장을 준수합니다.</span><span class="sxs-lookup"><span data-stu-id="e8ce1-190">NetX Duo SMTP Client API is compliant with RFC2821 “Simple Mail Transfer Protocol” and RFC 2554 “SMTP Service Extension for Authentication.</span></span> <span data-ttu-id="e8ce1-191">“</span><span class="sxs-lookup"><span data-stu-id="e8ce1-191">“</span></span>
